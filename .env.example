
# VoxCraft Environment Configuration Template
# Copy this file to .env and fill in your actual values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Primary database connection URL for Neon PostgreSQL
# Format: postgresql://username:password@hostname:port/database?sslmode=require
DATABASE_URL=postgresql://username:password@ep-example-123456.us-east-1.aws.neon.tech/voxcraft?sslmode=require

# Direct database connection (used by Drizzle ORM for migrations)
# Should be the same as DATABASE_URL in most cases
DIRECT_URL=postgresql://username:password@ep-example-123456.us-east-1.aws.neon.tech/voxcraft?sslmode=require

# =============================================================================
# EXTERNAL API CONFIGURATION
# =============================================================================

# ElevenLabs API Key (Required for voice generation)
# Get your API key from: https://elevenlabs.io/app/settings/api-keys
# Used for: Text-to-speech, voice cloning, and audio generation
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here

# OpenAI API Key (Required for AI content generation)
# Get your API key from: https://platform.openai.com/api-keys
# Used for: Script generation, dialogue creation, and content enhancement
OPENAI_API_KEY=sk-your_openai_api_key_here

# =============================================================================
# SUPABASE CONFIGURATION (Optional - for additional backend services)
# =============================================================================

# Supabase Project URL
# Format: https://your-project-id.supabase.co
# Used for: Additional authentication, real-time features, and edge functions
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous Key (Public key, safe to expose in client)
# Used for: Client-side Supabase operations and authentication
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your_supabase_anon_key

# Supabase Service Role Key (Server-side only, keep secret)
# Used for: Administrative operations and server-side database access
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your_service_role_key

# =============================================================================
# SESSION AND SECURITY CONFIGURATION
# =============================================================================

# Session secret for encrypting session data
# Generate a secure random string: openssl rand -base64 32
# IMPORTANT: Use a strong, unique secret in production
SESSION_SECRET=your_secure_session_secret_minimum_32_characters_long

# JWT Secret for token signing (if using JWT authentication)
# Generate a secure random string: openssl rand -base64 64
JWT_SECRET=your_jwt_secret_for_token_signing_should_be_very_long_and_secure

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Application environment
# Options: development, production, test
NODE_ENV=development

# Server port (Replit uses 5000 for external access)
# Note: In Replit, this must be 5000 for proper port forwarding
PORT=5000

# API Base URL for client-side requests
# Development: http://localhost:5000
# Production: https://your-repl-name.your-username.repl.co
VITE_API_URL=http://localhost:5000

# Application URL (used for CORS and redirects)
# Development: http://localhost:5000
# Production: https://your-repl-name.your-username.repl.co
APP_URL=http://localhost:5000

# =============================================================================
# AUDIO PROCESSING CONFIGURATION
# =============================================================================

# Maximum audio file size in MB
MAX_AUDIO_FILE_SIZE=50

# Supported audio formats (comma-separated)
SUPPORTED_AUDIO_FORMATS=mp3,wav,ogg,m4a

# Audio processing queue timeout in seconds
AUDIO_PROCESSING_TIMEOUT=300

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================

# Enable debug logging (development only)
DEBUG=false

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# Enable performance monitoring
ENABLE_PERFORMANCE_MONITORING=true

# Enable API request logging
ENABLE_API_LOGGING=true

# =============================================================================
# THIRD-PARTY SERVICE CONFIGURATION
# =============================================================================

# Analytics service (optional)
# GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX
# MIXPANEL_TOKEN=your_mixpanel_token

# Error tracking service (optional)
# SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# File storage service (optional)
# AWS_ACCESS_KEY_ID=your_aws_access_key
# AWS_SECRET_ACCESS_KEY=your_aws_secret_key
# AWS_S3_BUCKET=your-s3-bucket-name
# AWS_REGION=us-east-1

# =============================================================================
# SECURITY NOTES
# =============================================================================

# 1. Never commit actual API keys or secrets to version control
# 2. Use strong, unique passwords for database connections
# 3. Rotate API keys regularly
# 4. Use different secrets for different environments
# 5. Ensure SESSION_SECRET is at least 32 characters long
# 6. Enable SSL/TLS in production (sslmode=require for database)
# 7. Restrict database access to necessary IP addresses only

# =============================================================================
# GETTING STARTED
# =============================================================================

# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values (replace all placeholder values)
# 3. Ensure your database is accessible and credentials are correct
# 4. Obtain API keys from ElevenLabs and OpenAI
# 5. Test the configuration: npm run check
# 6. Start development: npm run dev

# =============================================================================
# TROUBLESHOOTING
# =============================================================================

# Database Connection Issues:
# - Verify DATABASE_URL format and credentials
# - Check if database server is running and accessible
# - Ensure SSL mode is configured correctly for your database

# API Key Issues:
# - Verify API keys are active and have necessary permissions
# - Check API key format (ElevenLabs vs OpenAI have different formats)
# - Ensure you have sufficient credits/quota for API usage

# Session Issues:
# - Ensure SESSION_SECRET is set and sufficiently long
# - Clear browser cookies if experiencing authentication issues
# - Check that session store (database) is accessible
